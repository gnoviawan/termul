# Termul Landing Page

A standalone Vite + React marketing page for Termul Manager.

## Development

```bash
npm install
npm run dev
```

The dev server runs from this `landing/` directory and does not affect the Tauri desktop app.

## Production Build

```bash
npm run build
npm run preview
```
