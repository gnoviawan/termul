// WorkspaceDashboard is now a simple empty component
// The actual workspace layout (sidebar, terminals, etc.) is handled by WorkspaceLayout
// This component exists for routing purposes but renders nothing
export default function WorkspaceDashboard(): React.JSX.Element {
  return <></>
}
